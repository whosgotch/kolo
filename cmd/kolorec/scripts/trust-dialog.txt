# A dialog is on screen and Enter would answer it.
#
# This is the state a guest message must never reach. An early version's very
# first line sent auto-answered "Yes, I trust this folder".
# Recorded in a fresh directory, which is what makes the agent ask.
waitfor 60s I trust this folder
wait 2s
dump dialog
